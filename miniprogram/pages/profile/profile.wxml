<view class="page">
  <view class="title">我的家庭博物馆身份</view>

  <block wx:if="{{user && family}}">
    <!-- 家庭信息 -->
    <view class="panel identity-panel">
      <view class="panel-title">我的家庭</view>
      <view class="identity-family-name">{{family.familyName}}</view>
    </view>

    <!-- 个人信息 -->
    <view class="panel identity-panel">
      <view class="panel-title">我的身份</view>
      <view class="identity-rows">
        <view class="row">
          <view class="row-label">我的称呼</view>
          <view class="row-value">{{user.name || '未设置'}}</view>
        </view>
        <view class="row">
          <view class="row-label">我的身份</view>
          <view class="row-value elder-badge">老东西</view>
        </view>
        <view class="row">
          <view class="row-label">家人怎么称呼我</view>
          <view class="row-value">{{user.relation || '未设置'}}</view>
        </view>
      </view>
    </view>

    <!-- 邀请码 -->
    <view class="panel invite-panel">
      <view class="panel-title">邀请家人一起修复</view>
      <view class="invite-desc">把邀请码发给家人，让他们一起补全这些老东西的故事。</view>
      <view class="invite-row">
        <view class="invite-code">{{inviteCode}}</view>
        <button class="copy-btn" bindtap="copyInviteCode">复制</button>
      </view>
    </view>

    <button wx:if="{{demoMode}}" class="reset-btn" bindtap="resetDemoData">恢复初始展品</button>
  </block>

  <!-- 未绑定家庭 -->
  <view wx:else class="empty">
    <view class="muted" style="margin-bottom: 28rpx;">暂未绑定家庭</view>
  </view>
</view>
